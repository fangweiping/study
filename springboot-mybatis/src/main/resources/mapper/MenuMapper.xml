<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.fwp.demo.dao.MenuMapper">

    <select id="queryMenu">
        select id,name,link,parent_id,order_num from t_menu order by order_num asc
    </select>

</mapper>
